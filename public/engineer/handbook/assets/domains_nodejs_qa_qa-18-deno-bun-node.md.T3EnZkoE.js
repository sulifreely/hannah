import{_ as n,o as s,c as l,ag as i}from"./chunks/framework.BbyMWZAR.js";const g=JSON.parse('{"title":"Deno vs Bun vs Node.js","description":"","frontmatter":{"title":"Deno vs Bun vs Node.js","difficulty":"medium","tags":["Deno","Bun","Node.js","运行时","对比"],"estimate":20},"headers":[],"relativePath":"domains/nodejs/qa/qa-18-deno-bun-node.md","filePath":"domains/nodejs/qa/qa-18-deno-bun-node.md","lastUpdated":1783504823000}'),e={name:"domains/nodejs/qa/qa-18-deno-bun-node.md"};function o(d,t,a,r,p,h){return s(),l("div",null,[...t[0]||(t[0]=[i(`<h1 id="问答-18-进阶-deno-vs-bun-vs-node-js" tabindex="-1">问答 18（进阶）：Deno vs Bun vs Node.js <a class="header-anchor" href="#问答-18-进阶-deno-vs-bun-vs-node-js" aria-label="Permalink to &quot;问答 18（进阶）：Deno vs Bun vs Node.js&quot;">​</a></h1><h2 id="题目" tabindex="-1">题目 <a class="header-anchor" href="#题目" aria-label="Permalink to &quot;题目&quot;">​</a></h2><p>Deno 和 Bun 都是后起的 JavaScript 运行时，它们和 Node.js 相比有什么不同？各自的设计目标、技术选型、生态成熟度如何？分别适合什么场景？</p><h2 id="参考答案" tabindex="-1">参考答案 <a class="header-anchor" href="#参考答案" aria-label="Permalink to &quot;参考答案&quot;">​</a></h2><p><strong>三者定位与出身</strong></p><table tabindex="0"><thead><tr><th>项目</th><th>创建者</th><th>发布时间</th><th>设计目标</th><th>底层引擎</th></tr></thead><tbody><tr><td><strong>Node.js</strong></td><td>Ryan Dahl</td><td>2009</td><td>让 JS 跑在服务端</td><td>V8</td></tr><tr><td><strong>Deno</strong></td><td>Ryan Dahl（Node 之父）</td><td>2018</td><td>重新思考 JS 运行时，解决 Node 的设计遗憾</td><td>V8</td></tr><tr><td><strong>Bun</strong></td><td>Jarred Sumner</td><td>2022</td><td>追求极致性能，All-in-one 工具链</td><td>JavaScriptCore (JSC)</td></tr></tbody></table><p><strong>设计哲学差异</strong></p><p><strong>Node.js</strong>：</p><ul><li>诞生早，很多设计是「历史产物」（CommonJS、<code>node_modules</code> 的包袱、回调地狱时代的 API 设计）。</li><li>目标是「能用」→ 生态起来了 → 历史包袱重 → 改起来慢。</li><li>优点：生态最成熟、稳定性高、企业级应用广泛。</li></ul><p><strong>Deno</strong>：</p><ul><li>Ryan Dahl 离开 Node 多年后，总结了 Node 的「十大遗憾」（安全、模块系统、package.json、node_modules、index.js、ts 支持等），重新做的一个运行时。</li><li>核心理念：<strong>安全默认</strong>（默认没有文件/网络/环境权限）、<strong>原生 TypeScript</strong>、<strong>浏览器兼容 API</strong>、<strong>去中心化模块</strong>（URL import，不用 npm install）、<strong>内置工具链</strong>（lint、test、fmt、bundle 都自带）。</li><li>目标是「做一个更好的现代 JS/TS 运行时」。</li></ul><p><strong>Bun</strong>：</p><ul><li>核心目标是<strong>快</strong>——启动快、运行快、安装快。</li><li>选择 JavaScriptCore（Safari 的引擎）而不是 V8，因为 JSC 启动更快、内存占用更小。</li><li>All-in-one：既是运行时，也是打包器、测试运行器、包管理器、转译器。</li><li>目标是「让 JS 开发的一切都变快」。</li></ul><p><strong>功能对比</strong></p><table tabindex="0"><thead><tr><th>特性</th><th>Node.js</th><th>Deno</th><th>Bun</th></tr></thead><tbody><tr><td>TypeScript 支持</td><td>需 ts-node / tsc / tsx 等</td><td>原生支持（内置转译）</td><td>原生支持（内置转译）</td></tr><tr><td>模块系统</td><td>CommonJS + ESM</td><td>ESM 优先（也支持 CJS）</td><td>ESM + CJS（兼容性好）</td></tr><tr><td>包管理</td><td>npm / yarn / pnpm + node_modules</td><td>URL import + 全局缓存（也支持 npm）</td><td>内置 bun install（兼容 npm 包，快）</td></tr><tr><td>安全模型</td><td>全权限（默认能读写文件、访问网络）</td><td>默认无权限，需显式 <code>--allow-read</code> 等</td><td>全权限（和 Node 类似）</td></tr><tr><td>标准库</td><td>有（fs/path/http 等），比较基础</td><td>丰富（deno/std，质量高）</td><td>有，兼容性优先</td></tr><tr><td>浏览器 API 兼容</td><td>部分（fetch、Blob 等逐步加）</td><td>高（Web API 优先设计）</td><td>中等</td></tr><tr><td>内置工具</td><td>少（node 本身只是运行时）</td><td>多（lint/fmt/test/bundle/doc/bench）</td><td>多（打包/测试/包管理/转译）</td></tr><tr><td>顶层 await</td><td>支持（ESM）</td><td>支持</td><td>支持</td></tr><tr><td>node_modules 依赖</td><td>是</td><td>可选（兼容 npm 时才需要）</td><td>是（但 bun install 很快）</td></tr></tbody></table><p><strong>性能对比（粗略）</strong></p><table tabindex="0"><thead><tr><th>维度</th><th>Node.js</th><th>Deno</th><th>Bun</th></tr></thead><tbody><tr><td>冷启动速度</td><td>一般</td><td>较快</td><td><strong>最快</strong>（JSC 优势）</td></tr><tr><td>运行时性能（CPU 密集）</td><td>好（V8 TurboFan 很强）</td><td>好（也是 V8）</td><td>中等（JSC 稍弱）</td></tr><tr><td>I/O 性能</td><td>好（libuv）</td><td>好（Tokio + rusty_v8）</td><td>好（自研 + JSC）</td></tr><tr><td>内存占用</td><td>较高</td><td>中等</td><td><strong>最低</strong></td></tr><tr><td>包安装速度</td><td>pnpm 已经很快了</td><td>-</td><td><strong>最快</strong>（声称比 pnpm 快 30 倍）</td></tr></tbody></table><p>注意：性能对比很依赖具体场景，不能一概而论。Bun 在启动速度、安装速度上优势明显；但在长时间运行的 CPU 密集任务上，V8（Node/Deno）的 JIT 优化可能更好。</p><p><strong>生态成熟度</strong></p><ul><li><strong>Node.js</strong>：遥遥领先。npm 上几百万个包，企业级框架（Express、Nest、Koa、Fastify 等）数不胜数，人才储备多，问题一搜就有答案。</li><li><strong>Deno</strong>：生态在成长中。可以用 npm 包（<code>npm:</code> 前缀），兼容性不断提升，但仍有部分包不兼容。有自己的标准库和一些 Deno 原生框架（如 Fresh、Oak），但企业级应用还不算多。</li><li><strong>Bun</strong>：生态更年轻。兼容性目标是「跑大多数 Node 项目」，而且进展很快，很多流行框架（如 Next.js、Nuxt、Vite）已经能在 Bun 上跑。但生产环境稳定性还需要时间验证。</li></ul><p><strong>适用场景</strong></p><p><strong>Node.js 适合</strong>：</p><ul><li>企业级生产服务（稳定性有保障）。</li><li>需要丰富生态和成熟方案的项目。</li><li>团队已经有 Node 技术积累。</li><li>大多数常规后端开发场景。</li></ul><p><strong>Deno 适合</strong>：</p><ul><li>新项目、小工具、脚本，想要现代干净的开发体验。</li><li>对安全性要求高的场景（比如跑用户提交的代码、不可信脚本）。</li><li>同时做前端和后端，想要一致的 Web API。</li><li>想要一个「零配置」的 TS 运行时（不用装一堆工具）。</li></ul><p><strong>Bun 适合</strong>：</p><ul><li>对启动速度敏感的场景（Serverless、CLI 工具、脚本）。</li><li>想要「一个工具搞定一切」（运行 + 打包 + 测试 + 包管理）。</li><li>前端工具链的提速（比如用 bun 跑测试、跑 vite 等）。</li><li>喜欢尝鲜、追求极致性能的个人项目。</li></ul><p><strong>现状与趋势</strong></p><ul><li>Node.js 仍然是绝对的主流和王者，短期不会动摇。</li><li>Deno 和 Bun 都在向「兼容 Node 生态」靠拢，因为没有人能从零建一个新生态。</li><li>三者互相借鉴：Node 也在加更多 Web API、原生支持 ESM；Deno 也开始支持 npm；Bun 也在补兼容性。</li><li>长期来看，Node 地位稳固，Deno 和 Bun 会在各自优势场景找到位置，不太可能完全取代 Node。</li></ul><h2 id="追问-1-deno-的「安全默认」具体指什么-有哪些权限-怎么授予" tabindex="-1">追问 1：Deno 的「安全默认」具体指什么？有哪些权限？怎么授予？ <a class="header-anchor" href="#追问-1-deno-的「安全默认」具体指什么-有哪些权限-怎么授予" aria-label="Permalink to &quot;追问 1：Deno 的「安全默认」具体指什么？有哪些权限？怎么授予？&quot;">​</a></h2><h3 id="答" tabindex="-1">答 <a class="header-anchor" href="#答" aria-label="Permalink to &quot;答&quot;">​</a></h3><p>Deno 的安全模型是「默认最小权限」——默认情况下，Deno 进程<strong>没有任何权限</strong>访问系统资源。这和 Node.js 形成鲜明对比（Node 默认能做任何事）。</p><p><strong>具体权限</strong>：</p><table tabindex="0"><thead><tr><th>权限标志</th><th>作用</th></tr></thead><tbody><tr><td><code>--allow-read</code></td><td>允许读取文件系统</td></tr><tr><td><code>--allow-write</code></td><td>允许写入文件系统</td></tr><tr><td><code>--allow-net</code></td><td>允许网络访问</td></tr><tr><td><code>--allow-env</code></td><td>允许读取环境变量</td></tr><tr><td><code>--allow-run</code></td><td>允许运行子进程</td></tr><tr><td><code>--allow-ffi</code></td><td>允许调用外部函数接口（动态库）</td></tr><tr><td><code>--allow-all</code> / <code>-A</code></td><td>允许所有权限（不推荐）</td></tr></tbody></table><p><strong>精细控制</strong>： 权限可以限定范围，不是全开：</p><div class="language-bash vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 只允许读取 /tmp 目录</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">deno</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --allow-read=/tmp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app.ts</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 只允许访问 example.com 的 443 端口</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">deno</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --allow-net=example.com:443</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app.ts</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 允许读写特定目录</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">deno</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --allow-read=./data</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --allow-write=./data</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app.ts</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p><strong>为什么重要</strong>：</p><ul><li>运行第三方代码时更安全——比如你跑一个别人写的脚本，它不能偷偷读你的文件、发你的数据。</li><li>符合最小权限原则（Principle of least privilege）。</li><li>对 Serverless、边缘计算场景特别有价值。</li></ul><p>Deno 还支持「权限提示」——如果代码尝试访问没授权的资源，Deno 会在终端问你要不要允许，可以交互授予。</p><p>当然，如果你信任你的代码，直接 <code>--allow-all</code> 也没问题，和 Node 一样。但 Deno 强制你「意识到」权限这件事。</p><h2 id="追问-2-bun-为什么选择-javascriptcore-而不是-v8-jsc-相比-v8-有什么优劣" tabindex="-1">追问 2：Bun 为什么选择 JavaScriptCore 而不是 V8？JSC 相比 V8 有什么优劣？ <a class="header-anchor" href="#追问-2-bun-为什么选择-javascriptcore-而不是-v8-jsc-相比-v8-有什么优劣" aria-label="Permalink to &quot;追问 2：Bun 为什么选择 JavaScriptCore 而不是 V8？JSC 相比 V8 有什么优劣？&quot;">​</a></h2><h3 id="答-1" tabindex="-1">答 <a class="header-anchor" href="#答-1" aria-label="Permalink to &quot;答&quot;">​</a></h3><p>这是 Bun 最有争议也最有意思的技术选型之一。</p><p><strong>为什么选 JSC</strong>： Bun 的作者 Jarred Sumner 选择 JavaScriptCore（WebKit 的 JS 引擎，Safari 在用）主要原因是：</p><ol><li><p><strong>启动速度更快</strong>：</p><ul><li>JSC 的启动时间比 V8 短很多，因为 JSC 的架构更轻量，不需要预热那么久。</li><li>对于 CLI 工具、Serverless 函数这种「启动频繁、运行时间短」的场景，启动速度至关重要。</li><li>Bun 号称启动速度比 Node.js 快 4 倍以上。</li></ul></li><li><p><strong>内存占用更低</strong>：</p><ul><li>JSC 的内存 footprint 比 V8 小。</li><li>对于 Serverless 等按量计费场景，内存就是钱。</li></ul></li><li><p><strong>JSC 本身质量很高</strong>：</p><ul><li>WebKit 团队维护了很多年，稳定性和性能都不错。</li><li>Safari 虽然常被吐槽「新 IE」，但引擎本身技术实力很强。</li></ul></li><li><p><strong>集成度</strong>：</p><ul><li>Bun 用 Zig 语言写的，和 JSC 的集成更方便（JSC 有 C API，叫 JavaScriptCore.h）。</li></ul></li></ol><p><strong>JSC 相比 V8 的劣势</strong>：</p><ol><li><p><strong>峰值性能稍弱</strong>：</p><ul><li>长时间运行的 CPU 密集任务，V8 的 TurboFan JIT 编译器通常比 JSC 的 FTL 更快。</li><li>但大多数 Web 服务是 I/O 密集，这个差距影响不大。</li></ul></li><li><p><strong>生态和工具链</strong>：</p><ul><li>V8 有 DevTools 协议、丰富的调试工具、Profiler 等。</li><li>JSC 也有，但工具生态不如 V8 丰富，Node 开发者熟悉的调试方式可能不兼容。</li></ul></li><li><p><strong>兼容性细节</strong>：</p><ul><li>虽然都是遵循 ECMAScript 标准，但 V8 和 JSC 在一些边缘行为、性能特性上有差异。</li><li>很多库是针对 V8 优化和测试的，在 JSC 上可能有微妙 bug。</li></ul></li><li><p><strong>WASM 性能</strong>：</p><ul><li>V8 的 Liftoff + TurboFan WASM 引擎非常成熟。</li><li>JSC 也支持 WASM，但性能和生态稍弱。</li></ul></li></ol><p><strong>总结</strong>： Bun 选 JSC 是一次「田忌赛马」——不和 V8 比峰值性能，而是在启动速度、内存占用这些 JSC 有优势的地方发力，瞄准 CLI、Serverless、前端工具链这些对启动速度敏感的场景。这是一个很聪明的定位，而不是想做「又一个 V8 运行时」。</p><h2 id="追问-3-deno-和-bun-都声称兼容-node-js-生态-它们是怎么做到的-兼容性真的-100-吗" tabindex="-1">追问 3：Deno 和 Bun 都声称兼容 Node.js 生态，它们是怎么做到的？兼容性真的 100% 吗？ <a class="header-anchor" href="#追问-3-deno-和-bun-都声称兼容-node-js-生态-它们是怎么做到的-兼容性真的-100-吗" aria-label="Permalink to &quot;追问 3：Deno 和 Bun 都声称兼容 Node.js 生态，它们是怎么做到的？兼容性真的 100% 吗？&quot;">​</a></h2><h3 id="答-2" tabindex="-1">答 <a class="header-anchor" href="#答-2" aria-label="Permalink to &quot;答&quot;">​</a></h3><p>两者都花了很大力气做 Node 兼容，但原理和程度不同，也都做不到 100%。</p><p><strong>Deno 的 Node 兼容方式</strong>：</p><ol><li><strong>早期</strong>：Deno 完全走 URL import 路线，和 Node 生态割裂，结果开发者不买账（生态太重要了）。</li><li><strong>后来</strong>：Deno 增加了 <code>npm:</code> 协议和 Node 兼容层（<code>deno.ns</code>、<code>node:</code> 内置模块）。</li></ol><div class="language-ts vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Deno 里直接用 npm 包</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> lodash </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;npm:lodash&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> express </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;npm:express@4.18.2&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br></div></div><ol start="3"><li><p><strong>兼容层实现</strong>：</p><ul><li>Deno 用 Rust 实现了 Node.js 核心模块的兼容版本（fs、path、http、stream、buffer 等）。</li><li>用 <code>node:</code> 前缀导入时走兼容层。</li><li>npm 包下载后存在全局缓存，和 pnpm 的 store 类似。</li><li>兼容 CommonJS（有自己的 CJS 实现）。</li></ul></li><li><p><strong>兼容性</strong>：</p><ul><li>大多数纯 JS 的 npm 包能跑。</li><li>但一些依赖 Node 内部细节、原生插件（<code>.node</code> 文件）、冷门 API 的包可能跑不起来。</li><li>Deno 有兼容测试套件，不断提升覆盖率。</li></ul></li></ol><p><strong>Bun 的 Node 兼容方式</strong>：</p><ol><li><p><strong>设计目标就是兼容 Node</strong>：Bun 从一开始就把「能跑大多数 Node 项目」作为核心目标。</p></li><li><p><strong>实现方式</strong>：</p><ul><li>Bun 用 Zig + C++ 实现了 Node.js 核心 API 的大部分。</li><li>内置了 CommonJS 和 ESM 的加载器。</li><li>内置了 node_modules 的解析逻辑（和 pnpm 类似但更快）。</li><li>支持 <code>node:</code> 前缀和不带前缀的导入。</li><li>甚至支持一些 Node 没正式文档化的内部 API（因为很多库依赖它们）。</li></ul></li><li><p><strong>兼容性</strong>：</p><ul><li>Bun 的 Node 兼容性比 Deno 更好一些（因为是核心目标）。</li><li>很多流行框架（Next.js、Nuxt、Vite、Astro 等）都能直接跑。</li><li>但仍然有不兼容的地方：一些冷门 API、原生 C++ 插件、依赖 V8 特定行为的代码。</li></ul></li></ol><p><strong>为什么做不到 100% 兼容</strong>：</p><ol><li><p><strong>底层引擎不同</strong>：</p><ul><li>Deno 也是 V8，这方面差异小一些。</li><li>Bun 是 JSC，很多 V8 特定的 API（如 <code>v8</code> 模块、某些 <code>--</code> 启动参数）没有。</li></ul></li><li><p><strong>原生模块（.node）</strong>：</p><ul><li>很多库用 C++ 写的原生插件，依赖 Node 的 N-API 或 V8 API。</li><li>Deno 有自己的 FFI 系统，和 Node 的原生模块不兼容。</li><li>Bun 正在努力支持 N-API，但也不是全部都能用。</li></ul></li><li><p><strong>未文档化的行为</strong>：</p><ul><li>很多 npm 包依赖 Node 的「巧合行为」（bug 或未定义行为）。</li><li>要完全兼容就得把这些 bug 也复刻出来，工作量巨大。</li></ul></li><li><p><strong>异步 I/O 实现不同</strong>：</p><ul><li>Node 用 libuv，Deno 用 Tokio（Rust 异步运行时），Bun 用自研的。</li><li>虽然对外 API 一样，但内部时序、错误处理细节可能有差异。</li></ul></li></ol><p><strong>实际建议</strong>：</p><ul><li>如果是新项目、不依赖奇怪的包，Deno/Bun 大概率能用。</li><li>如果是老项目、深度依赖 Node 生态，先评估兼容性，不要贸然迁移。</li><li>可以用它们各自的「兼容性测试」工具或官方兼容列表来评估。</li><li>大多数情况下，「试一下就知道了」——跑一下你的测试套件，看看哪些挂了。</li></ul><h2 id="参考资料" tabindex="-1">参考资料 <a class="header-anchor" href="#参考资料" aria-label="Permalink to &quot;参考资料&quot;">​</a></h2><blockquote><p>深入阅读请从标注 <strong>首选</strong> 的来源开始；本领域完整索引见 <a href="/engineer/handbook/domains/nodejs/RESOURCES">Node.js 参考资料</a>。</p></blockquote><ul><li><strong>首选</strong> — <a href="https://nodejs.org/en/docs/" target="_blank" rel="noreferrer">Node.js Docs</a>：Node 运行时、模块生态与长期维护策略的基准参照。</li><li><a href="https://docs.deno.com/runtime/manual" target="_blank" rel="noreferrer">Deno Manual</a>：权限模型、原生 TypeScript 与 npm 兼容方案。</li><li><a href="https://bun.sh/docs" target="_blank" rel="noreferrer">Bun Docs</a>：JSC 引擎、内置打包器与 Node 兼容层设计目标。</li></ul>`,64)])])}const c=n(e,[["render",o]]);export{g as __pageData,c as default};
