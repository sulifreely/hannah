---
import ViewCount from './ViewCount.astro';

interface Props {
  url: string;
  slug: string;
  title: string;
  date: Date;
  description: string;
  tags?: string[];
  cover?: string;
}

const { url, slug, title, date, description, tags = [], cover } = Astro.props;

const dateStr = new Intl.DateTimeFormat('zh-CN', {
  year: 'numeric',
  month: '2-digit',
  day: '2-digit',
}).format(date);
---

<article class="post-card">
  <div class="card-body">
    <div class="meta">
      <time datetime={date.toISOString()}>{dateStr}</time>
      <ViewCount slug={slug} contentType="blog" mode="readonly" />
      {tags.length > 0 && (
        <span class="tags">
          {tags.map((tag) => <span class="tag">#{tag}</span>)}
        </span>
      )}
    </div>
    <h2 class="title">
      <a href={url}>{title}</a>
    </h2>
    <p class="desc">{description}</p>
  </div>
  {cover && (
    <a class="thumb" href={url} aria-hidden="true" tabindex="-1">
      <img src={cover} alt="" loading="lazy" />
    </a>
  )}
</article>

<style>
  .post-card {
    display: flex;
    align-items: flex-start;
    gap: clamp(1.25rem, 3vw, 2rem);
    padding: 1.75rem 0;
    border-bottom: 1px solid var(--border);
  }
  .card-body {
    flex: 1;
    min-width: 0;
  }
  .thumb {
    flex-shrink: 0;
    display: block;
    width: clamp(180px, 30vw, 260px);
    border-radius: 4px;
    overflow: hidden;
    border: 1px solid var(--border);
    box-shadow: 3px 3px 0 var(--border);
  }
  .thumb img {
    display: block;
    width: 100%;
    height: auto;
    background: var(--paper);
  }
  @media (max-width: 680px) {
    .thumb {
      width: 136px;
    }
  }
  @media (max-width: 560px) {
    .post-card {
      flex-direction: column;
      align-items: stretch;
      gap: 0.85rem;
    }
    .thumb {
      order: -1;
      width: 100%;
    }
  }
  .meta {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    font-family: var(--font-mono);
    font-size: 0.8rem;
    color: var(--fg-subtle);
    margin-bottom: 0.5rem;
  }
  .tags {
    display: inline-flex;
    flex-wrap: wrap;
    gap: 0.4rem;
  }
  .tag {
    color: var(--accent);
    background: var(--tag-bg);
    padding: 0.1em 0.55em;
    border-radius: 3px;
  }
  .title {
    margin: 0 0 0.4rem;
    font-size: 1.3rem;
  }
  .title a:hover {
    text-decoration: none;
    color: var(--accent-hover);
  }
  .desc {
    margin: 0;
    color: var(--fg-muted);
    font-size: 0.97rem;
    line-height: 1.65;
  }
</style>
